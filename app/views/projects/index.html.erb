<p style="color: green"><%= notice %></p>

<h1>Projects (<%= @projects.count %>)</h1>

<div id="projects">
  <% @projects.each do |project| %>
    <%= render_project project %><br/>
    <%#jj <p> < %= link_to "Show this project", project % > </p> %>
  <% end %>
</div>

<%= link_to "New project", new_project_path %>
