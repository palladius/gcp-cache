<p style="color: green"><%= notice %></p>

<h1>🗂️ Organizations</h1>



<div id="folders">
  <% @folders.each do |folder| %>
    <p>
      <%= render_folder(folder) %>
    </p>
  <% end %>
</div>
