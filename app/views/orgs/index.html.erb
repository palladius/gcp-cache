<p style="color: green"><%= notice %></p>

<h1>🗂️ <%=@folders.count%> Organizations</h1>


<div id="folders">
  <table class="table" >
    <thead class="thead-light" >
      <%= render_org_as_tr(nil) %>
    </thead>
    <tbody>
      <% @folders.each do |folder| %>
        <%= render_org_as_tr(folder) %>
      <% end %>
    </tbody>

  </table>
</div>
