#rake db:fixtures:load FIXTURES_DIR=db/fixtures/manhouse/
# https://brandonhilkert.com/blog/using-rails-fixtures-to-seed-a-database/
# FIXTURES_DIR=$(pwd)/db/fixtures/manhouse/ rake db:fixtures:load
test1:
  gcp_k: RiccFixtureDir
  gcp_val: db/fixtures/manhouse/labels.yml
#  gcp_value: sobenme
# not sure if this works.
seedVersion2:
  gcp_k: seedVersion2
  gcp_val: "#{SeedVersion}"